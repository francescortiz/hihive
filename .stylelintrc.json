{
  "extends": "stylelint-config-standard",
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["layer", "apply", "variants", "responsive", "screen"]
      }
    ]
  }
}