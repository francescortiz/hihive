// Prerender todo el sitio a HTML estático en build time.
// csr: true mantiene la interactividad Svelte en cliente (lightbox, modales, reveal).
export const prerender = true;
export const ssr = true;
export const csr = true;